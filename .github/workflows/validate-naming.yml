name: Validate Patch Naming

on:
  pull_request:
    paths:
      - 'patches/**'
      - 'metadata/**'

jobs:
  validate-naming:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pyyaml

      - name: Validate patch naming
        id: validate
        run: |
          python scripts/validate_filenames.py --pr --json > validation_result.json
          cat validation_result.json

      - name: Generate PR comment
        if: always()
        run: |
          python -c "
          import json
          
          with open('validation_result.json') as f:
              result = json.load(f)
          
          if result['valid']:
              comment = '''## 🏷️ Filename Validation Report
          
          ✅ **All filenames follow the naming convention!**
          
          No issues detected.'''
          else:
              issues = result['issues']
              comment = f'''## 🏷️ Filename Validation Report
          
          ⚠️ **Non-compliant filenames detected ({len(issues)})**
          
          | Current Filename | Expected Filename | Metadata |
          |------------------|-------------------|----------|'''
              
              for issue in issues:
                  comment += f'''
          | \`{issue['current']}\` | \`{issue['expected']}\` | \`{issue['metadata']}\` |'''
              
              comment += '''
          
          ### 📋 How to fix
          
          Run the rename script locally:
          \`\`\`bash
          python scripts/rename_patches.py --apply
          git add patches/ metadata/
          git commit -m \"chore: standardize patch filenames\"
          git push
          \`\`\`
          
          **Note**: This check is informational only and will not block the PR merge.'''
          
          with open('pr_comment.md', 'w') as f:
              f.write(comment)
          "

      - name: Comment PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const comment = fs.readFileSync('pr_comment.md', 'utf8');
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });